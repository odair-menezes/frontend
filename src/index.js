import React from "react"; // Importa o React
import ReactDOM from "react-dom"; // Para manipular o DOM

import App from "./App";

ReactDOM.render(<App />, document.getElementById("root")); // Renderizando alguma coisa dentro da DIV com o ID roo
